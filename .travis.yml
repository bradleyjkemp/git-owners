language: go
before_install:
  - go get github.com/mattn/goveralls
jobs:
  include:
    - stage: test
      script: $GOPATH/bin/goveralls -service=travis-ci
    - stage: owners
      script: exit 1
    
notifications:
  email: false
